[project]
name = "math341-final-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "hansil", email = "utahnu@byu.edu" }
]
requires-python = ">=3.12"
dependencies = [
    "ipynb>=0.5.1",
    "jupyter>=1.1.1",
    "matplotlib>=3.11.1",
    "numpy>=2.5.1",
]

[project.scripts]
math341-final-project = "math341_final_project:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
